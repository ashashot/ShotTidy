//
//  ContentView.swift
//  ShotTidy
//
//  Не используется — главный экран: CatalogView (через ShotTidyApp.swift)
//  Можно удалить через Xcode.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        CatalogView()
    }
}
